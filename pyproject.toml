[project]
name = "cpumon"
version = "0.1.0"
description = "Tool to monitor CPU and set min / max frequencies"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.20.0",
    "pyside6>=6.11.0",
    "superqt>=0.8.1",
]
