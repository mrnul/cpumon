# cpumon

Simple python tool (with PySide6) to monitor cpu usage and set min / max scaling frequencies

Basic support for profiles with some examples in profiles.json

I use this on linux mint to keep cpu temperatures low
